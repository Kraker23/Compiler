
using MetroFramework.Controls;
using MetroFramework.Forms;

namespace Compiler.UI
{
    public partial class frmMain : MetroForm
    {
        public frmMain()
        {
            InitializeComponent();

            /*
            managerReportApi = TUVject.Instance.ServiceProvider.GetService<IInspeccionReportManager>(); 
            managerQueueApi = TUVject.Instance.ServiceProvider.GetService<IInspectionQueueData>(); 
            inspeccionLineaFicheroData = TUVject.Instance.ServiceProvider.GetService<IInspeccionLineaFicheroData>();
            fileManager = TUVject.Instance.ServiceProvider.GetService<ILineInspectionFileManager>();
            conf = TUVject.Instance.ServiceProvider.GetService<IConfiguracionData>();
            tipoContenido = TUVject.Instance.ServiceProvider.GetService<ITipoContenidoData>();
           */
        }
    }
}
